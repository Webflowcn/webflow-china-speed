# 分支索引（2026-09-08）

发布前已执行 git fetch 并通过 GitHub API 核对：本地 12 个分支，GitHub 10 个分支；没有打开中的 PR。下表为整理前快照。本次保留所有历史分支，不删除备份。

## 当前开发入口

- `main`：本轮由 `00f9cd2`（v2.4）升级到 v2.6；后续开发应从新 main 创建分支。
- `feat/v2.6-tencent-acceleration`：发布前最新功能分支，`3324b71`，2026-08-24；比旧 main 多 5 个提交。本轮内容全部纳入 main，不再作为新的开发基线。
- `codex/release-v2.6.0`：本次发布准备使用的临时本地分支，仅补充版本和分支说明；不需要长期保留。

## 发布前分支盘点

| 分支 | 本地 / GitHub | 提交 | 含义与建议 |
|---|---|---|---|
| main | 两边都有 | 00f9cd2 | 发布前正式代码基线 v2.4；本轮升级 |
| feat/v2.6-tencent-acceleration | 两边都有 | 3324b71 | 最新候选，包含 v2.5 与 v2.6；本轮纳入 main |
| feat/edgeone-kv-snapshot-v2.4 | 两边都有 | 00f9cd2 | KV 持久快照开发分支，与旧 main 完全相同，已合入 |
| backup/pre-v2.6-tencent-acceleration | 两边都有 | 20e809d | v2.6 之前的 v2.5 Blob 基线，包含 Blob 备用快照，并非旧 main 的副本 |
| backup/pre-v2.4-kv-snapshot-20260824 | 仅本地 | 18d3fa7 | KV 快照之前的 v2.3.1 基线，含浏览器审计退出等待修复 |
| backup/pre-v2.3.1-edgeone-20260823 | 两边都有 | 97a765f | v2.3.1 修复之前，已包含 v2.3 显式缓存与诊断 |
| backup/pre-v2.3-edgeone-20260823 | 两边都有 | 7a24873 | v2.3 缓存改造前的历史基线，末次提交为许可证 |
| codex/proxy-stream-optimize | 仅本地 | 3972b25 | v2.1 合并节点，涉及 CF 配额优化和 EdgeOne 目录整理；已包含在 main 历史中 |
| codex/stream-css-fix | 两边都有 | e37efd3 | 历史流式/CSS 工作分支，末次提交名为“首次提交”，但并非新版本；已包含在 main 历史中 |
| optimize/cf-quota-edgeone-cleanup | 两边都有 | 03dc710 | v2.1 配额优化、目录整理、默认源站；已包含在 main 历史中 |
| stream-fix | 两边都有 | ef1c707 | 2026-07-14 环境变量统一与目录清理；已包含在 main 历史中 |
| v2.0-geo-fix | 两边都有 | 052c73d | 2026-06-09 地理跳转、健康检查与缓存分离；已包含在 main 历史中 |

所有原有分支顶端均为 `3324b71` 的祖先或与它相同，没有游离在最新候选之外的独立提交。分支名中的 backup/pre 表示“该次升级之前的回退点”，不是待合并功能。

## 本轮 main 新增内容

1. v2.5：显式启用的 Blob HTML 备用快照，KV 优先；esbuild 打包依赖；预览门禁 Cookie 隔离。
2. v2.6：PUBLIC_HOST 与受信密钥回源，让 HTML、Canonical、资源和 Sitemap 使用正确外部域名。
3. 缓存诊断：STORE_OK/STORE_FAILED、资源分类；追踪参数归一化，功能参数继续绕过公共缓存。
4. 主动刷新可从 Sitemap 最多发现 20 个页面；新增静态资源重复请求审计。
5. 腾讯云三线路测试配置模板、静态缓存规则和历史验收记录。

CF Worker 代码没有随本轮发布改变。此前讨论的“阿里云 DNS 分流”和“ESA 替代方案”尚未实施，不属于 v2.6 更新。

## 验证与发布边界

- 2026-09-08：EdgeOne 39 项本地测试通过，独立依赖安装和构建通过。
- 2026-08-24 的历史测试显示热缓存明显受益，冷节点首次回源仍约 2 秒；不承诺所有线路、每次访问都提速。
- GitHub main 更新不等于已核验客户生产域名升级。云平台若配置 main 自动构建，可自行触发部署，应以各平台部署状态和线上检查为准。
- zcode 的 2026-09-03 报告、架构图及 reasonix.toml 在原工作目录尚未提交，本轮没有纳入；不能通过切到其他分支找回这些文件，应保留原工作目录。

## 后续清理建议

确认 main 后，可删除已合入的普通功能分支；备份分支可先转换为带说明的 Git tag，再删除同名分支。当前未执行此清理，以保留用户现有回退入口。
